var app = angular.module('spheresuite')
app.controller('invoiceController', invoiceController);

invoiceController.$inject = ['$scope', '$rootScope'];

function invoiceController($scope, $rootScope) {
    var invoiceControllerScope = this;
    
} 